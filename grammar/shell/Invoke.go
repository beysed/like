package shell

/*
for _, e := range os.Environ() {
        pair := strings.SplitN(e, "=", 2)
        fmt.Println(pair[0])
    }

*/

// func sleep

func Invoke(command string, args ...[]string) error {
	return nil
}
